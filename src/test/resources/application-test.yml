spring:
  application:
    name: SocialMedia

  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: h2
    password:

  spring:
    jpa:
      hibernate:
        ddl-auto: update
      show-sql: true

logging:
  level:
    org:
      springframework:
        #        security: DEBUG
        security: INFO

SecretKey: AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA
